app: module = APP

Vector: struct = {
    x: f32,
    y: f32,
    new: fn (x: f32, y: f32) -> Vector = return Vector{.x = x, .y = y};
}

takes_foo: fn -> f64 = {
    vec: ^Vector = gcnew Vector::new(10, 20);
    return vec.x;
} 
