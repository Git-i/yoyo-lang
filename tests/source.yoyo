app: module = APP

test_views: fn (arr: [char:&mut]) = {
    *arr[1] = '😁';
}

takes_foo: fn -> f64 = {
    array: mut [char; 5] = ['H', 'o', 'l', 'l', 'o'];
    idx: mut = 0;
    test_views(&mut array, 40);
    while(idx < 5) {
        app::func(&"${*array[idx]}");
        idx = idx + 1;
    }
    return 10.0;
} 