Type: struct = {
    #(destructor)
    destroy: fn(&mut this) = {
        test::print(&"Destroyed type obj");
    }
}
call_yoyo_fn: fn = {
    test::panic();
}
panic: fn(xc: i32, flg: i32, narg: i32, arg: ptr) = c_import!("RaiseException");

function_that_panics: fn = {
    panic(7090, 0, 0, "asds".c_str());
    test::print(&"unreachable");
}
main: fn = {
    function_that_panics();
    test::print(&"got here somehow");
}
