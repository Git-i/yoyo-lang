app: module = APP

takes_foo: fn -> f64 = {
    a: mut (i32 | str) = "Hello";

    if |&string| (a as str) {
        *string = "World";
    }
    if |&string| (a as str) {
        app::func(string);
    }
    return 10;
}
