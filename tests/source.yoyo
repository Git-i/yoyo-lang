func_2: fn = {
	a: mut = Vector2{.x = 10, .y = 20};
	test::print(&"${(&a)[0]}, ${(&a)[1]}");
	test::print(&"${(&mut a)[0]}, ${(&mut a)[1]}");
}

Vector2: struct = {
	x: u32, y: u32
}

operator: [](x: &Vector2, y: u32) -> u32 = {
	if (y == 0) return x.x; else return x.y;
}
operator: mut [](x: &mut Vector2, y: u32) -> u32 = {
	return 100;
}
